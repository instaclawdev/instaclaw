{
  "name": "@instaclaw/realtime",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsc -w -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "fastify": "^4.22.0",
    "fastify-websocket": "^6.0.0",
    "@instaclaw/config": "workspace:*",
    "@instaclaw/logger": "workspace:*"
  }
}
